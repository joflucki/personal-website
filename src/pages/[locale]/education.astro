---
import type { GetStaticPaths } from "astro";
import BaseHead from "../../components/BaseHead.astro";
import Footer from "../../components/Footer.astro";
import Header from "../../components/Header.astro";
import { SITE_TITLE } from "../../consts";
import { LANGUAGES } from "../../i18n/ui";
import { toBCP47, useTranslations } from "../../i18n/utils";
import { Image } from "astro:assets";
import HEARC1 from "../../assets/images/education/e24da13_1656954766143-344659230-highres.jpg";
import HEARC2 from "../../assets/images/education/UNINE_neuchatel_cafiso_00001image.jpg";
import HESSO1 from "../../assets/images/education/vue-palais-rumine-cathedrale-5479.2022-03-08-09-09-41.jpg";
import HESSO2 from "../../assets/images/education/thomas-despeyroux-Dh5pUa2PwrY-unsplash.jpg";

export const getStaticPaths = (() => {
	const availablesLocales = Object.keys(LANGUAGES) as [
		keyof typeof LANGUAGES,
	];
	return availablesLocales.map((locale) => ({
		params: { locale },
	}));
}) satisfies GetStaticPaths;

const { locale } = Astro.params;
const t = useTranslations(locale);
---

<!doctype html>
<html lang={toBCP47(locale)}>
	<head>
		<BaseHead title={SITE_TITLE} />
		<style>
			main {
				max-width: 960px;
				margin: auto;
			}
			section {
				margin-bottom: 3em;
			}
			.picture-container {
				display: grid;
				grid-template-columns: auto auto;
				gap: 0.5em;
			}
			.picture-container img {
				border-radius: 6px;
				width: 100%;
				height: 100%;
				object-fit: cover;
				aspect-ratio: 16 / 9;
			}
			.copyright {
				color: var(--gray-light);
			}

			@media (max-width: 720px) {
				.picture-container {
					grid-template-columns: auto;
				}
			}
		</style>
	</head>
	<body>
		<Header />
		<main>
			<h1>{t("nav.education")}</h1>
			<section>
				<h2>{t("education.master-title")}</h2>
				<p>
					<b>{t("education.master-school")}</b>
				</p>
				<p>{t("education.master-description-1")}</p>
				<p>{t("education.master-description-2")}</p>
				<div class="picture-container">
					<Image src={HESSO1} alt="" />
					<Image src={HESSO2} alt="" />
				</div>
				<p class="copyright">
					&copy; {t("education.city-of-lausanne")} / Thomas Despeyroux
				</p>
			</section>

			<section>
				<h2>{t("education.bachelor-title")}</h2>
				<p>
					<b>{t("education.bachelor-school")}</b>
				</p>
				<p>{t("education.bachelor-description-1")}</p>
				<p>{t("education.bachelor-description-2")}</p>
				<div class="picture-container">
					<Image src={HEARC1} alt="" />
					<Image src={HEARC2} alt="" />
				</div>
				<p class="copyright">
					&copy; Valentin Flauraud / UniNE
				</p>
			</section>
		</main>
		<Footer />
	</body>
</html>
